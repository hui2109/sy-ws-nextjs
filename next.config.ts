import type {NextConfig} from "next";

const nextConfig: NextConfig = {
    /* config options here */
    // async redirects() {
    //     return [
    //         {
    //             source: '/',
    //             destination: '/scheduleTools/start',
    //             permanent: false,   // false = 302 临时重定向，开发阶段推荐
    //         },
    //     ];
    // },
};

export default nextConfig;
